# Copy to .env.local to override. All optional.

# Set to "false" to disable dev auto-login and show the login form.
VITE_AUTO_LOGIN=true

# Credentials used for auto-login (the seeded demo user).
VITE_DEMO_EMAIL=demo@swing-stack.dev
VITE_DEMO_PASSWORD=demo-password-123
